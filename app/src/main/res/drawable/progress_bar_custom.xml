<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#E0E0E0"/> <!-- Color de fondo (gris claro) -->
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <solid android:color="@color/STomas"/> <!-- Color de progreso -->
            </shape>
        </clip>
    </item>
</layer-list>
